interface themeInterface {
  buttons: {
    primary: {
      background: string;
      textColor: string;
      borderRadius: string;
      textAlign: string;
      fontSize: string;
      fontWeight: number;
      textDecoration: string;
      textTransform: string;
      padding: string;
    };
  };
}
export default themeInterface;
